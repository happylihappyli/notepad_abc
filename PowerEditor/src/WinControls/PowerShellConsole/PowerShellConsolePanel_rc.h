#pragma once
// 资源ID：PowerShell 控制台面板
// 将此头文件同时包含到 .rc 和 .h/.cpp 中以共享一致的ID定义

// 面板对话框ID
#define IDD_POWERSHELL_PANEL            4500

// 控件ID
#define IDC_PS_INPUT                    (IDD_POWERSHELL_PANEL + 1)
#define IDC_PS_RUN                      (IDD_POWERSHELL_PANEL + 2)
#define IDC_PS_STOP                     (IDD_POWERSHELL_PANEL + 3)
#define IDC_PS_CLEAR                    (IDD_POWERSHELL_PANEL + 4)
#define IDC_PS_OUTPUT                   (IDD_POWERSHELL_PANEL + 5)

